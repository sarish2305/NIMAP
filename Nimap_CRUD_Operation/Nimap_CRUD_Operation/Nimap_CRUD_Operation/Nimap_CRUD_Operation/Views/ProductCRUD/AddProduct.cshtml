@model IEnumerable<Nimap_CRUD_Operation.Models.ProductMaster>

@{
    ViewBag.Title = "AddProduct";
}

<h2>AddProduct</h2>

<p>
    @Html.ActionLink("Create New", "CreateProduct")
</p>
<table class="table" styel="border: 1px solid;">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProductName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProductName)
        </td>
        <td>
          
            @Html.ActionLink("Edit", "ProductEdit", new { id = item.ProductID }) |

            @Html.ActionLink("Delete", "ProductDelete", new { id = item.ProductID })
        </td>
    </tr>
}

</table>
